# /apps/base/authelia/base-release.yaml
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: authelia
  namespace: auth
spec:
  interval: 12h
  chart:
    spec:
      chart: authelia
      version: 0.9.14
      sourceRef:
        kind: HelmRepository
        name: authelia
        namespace: auth
      interval: 12h
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: vikunja-ingress
  namespace: vikunja
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`${VIKUNJA_DOMAIN}`)
    kind: Rule
    services:
    - name: vikunja
      port: 3456
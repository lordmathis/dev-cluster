apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: lemma
  namespace: lemma
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`lemma.nmsny.dev`)
      kind: Rule
      services:
        - name: lemma
          port: 80
